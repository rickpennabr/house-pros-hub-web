import LoadingOverlay from '@/components/ui/LoadingOverlay';

/**
 * Route-level loading UI for (auth) segment (signin, signup, forgot-password, etc.).
 */
export default function AuthLoading() {
  return <LoadingOverlay />;
}
