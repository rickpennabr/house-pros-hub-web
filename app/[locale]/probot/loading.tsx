import LoadingOverlay from '@/components/ui/LoadingOverlay';

/**
 * Route-level loading UI for probot segment (shown while probot layout is loading).
 */
export default function ProBotSegmentLoading() {
  return <LoadingOverlay />;
}
